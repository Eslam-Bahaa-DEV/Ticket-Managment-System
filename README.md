C++ console application that implements a basic Ticket Management System to simulate customer support operations. It allows user registration, ticket submission, processing tickets using a FIFO queue, and maintaining a resolved tickets archive. The system also supports searching tickets by user ID or ticket ID.

The project focuses on core Data Structures concepts such as linked lists, queues, and dynamic memory management.
